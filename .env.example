# Used by Compose to configure the API. Do not commit .env.
JWT_SECRET=replace-with-a-long-random-secret

# Used when running Nest directly from your machine, not inside Compose.
DATABASE_URL=some_url
# REDIS_URL=redis://localhost:6379
